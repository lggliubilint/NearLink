name: nearlink_monitor
description: StarLight Multi-Device Health Monitor with 3D Avatar
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  fl_chart: ^0.68.0
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.0
  flutter_screenutil: ^5.9.0
  flutter_local_notifications: ^18.0.0
  vibration: ^3.1.0
  model_viewer_plus: ^1.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
